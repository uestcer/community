name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Hi uestcer! Welcome to the Open Source Community! Hope that you can learn and grow here :)'
        pr-message: 'Hi uestcer! Welcome to the Open Source Community! Hope that you can learn and grow here :)'
